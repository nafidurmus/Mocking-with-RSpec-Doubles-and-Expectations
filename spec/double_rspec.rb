feed = double 

feed = double("feed")

allow(feed).to receive(:fetch).and_return("imagine I'm a JSON stong")
feet.fetch

comment = double("comment")
excect(Comment).to receive(:find).and_return(comment)